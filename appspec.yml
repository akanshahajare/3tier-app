version: 0.0
os: linux
files:
  - source: /tier2-app/
    destination: /home/ec2-user/deployment/tier2-app/
    overwrite: true
  - source: /tier1-frontend/
    destination: /var/www/3tier-app/
    overwrite: true
hooks:
  BeforeInstall:
    - location: scripts/before_install.sh
      timeout: 60
      runas: root
  ApplicationStart:
    - location: scripts/app_start.sh
      timeout: 120
      runas: root
